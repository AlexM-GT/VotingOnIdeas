{
  "inputs": [
    {
      "id": "figmaApiKey",
      "type": "promptString",
      "description": "Figma API key",
      "password": true
    }
  ],
  "servers": {
    "playwright": {
      "type": "stdio",
      "command": "npx",
      "args": [
        "-y",
        "@playwright/mcp@latest"
      ]
    },
    "figma": {
      "command": "npx",
      "args": [
        "-y",
        "figma-developer-mcp",
        "--stdio",
        "--figma-api-key=${input:figmaApiKey}"
      ]
    }
  }
}
